# Limina Architecture Reconstruction Audit — 2026-09-11

[English](./limina-architecture-audit.md) | [简体中文](./zh/limina-architecture-audit.md)

This page records evidence and reconciliation for this reconstruction; it does not independently define system semantics. Current definitions belong to the [system model](./limina-system-model.md), [semantics](./limina-semantics.md), and [lifecycle](./limina-lifecycle.md); properties belong to [I01–I12](./limina-invariants.md).

## Scope and method

The subject was the **dirty working tree** at the start of this task. HEAD `561683d2f11985eea85d0921ed2ed1a2768f48da` identifies only the baseline origin, not the staged semantic repairs. The baseline included the user's staged `.agents/docs/limina.md`, EN/ZH dependency graph articles, production semantic/build-graph/import-analysis changes and regression tests, plus untracked `a.ts` and `b.ts`.

Status/index/content fingerprints were recorded first. Call chains were reconstructed from CLI/config → pipeline/preflight → workspace/ownership → semantic facts → graph/validation → mutation/reporting, then challenged with tests, installed compiler oracles, and external reproductions before comparing the old PCR paragraph by paragraph. Memory supplied only working boundaries and retrieval hints, not technical facts or product direction. No production behavior changed, no tests were added or weakened, and nothing was staged or committed.

**Source-established**: Phases, fields, guards, and call boundaries in this record set come from direct source inspection. **Test-backed**: Existing regressions actually executed below. **Empirically verified**: The specific conditions listed in the external reproductions. **Derived**: Cache/API integration risks. **NOT VERIFIED**: The complete optional checker version matrix, physical Windows execution, remote CI, arbitrary concurrent filesystem races, and integration/build/release checks that were not run.

## PCR reconciliation

The review covered `.agents/docs/README.md`, `limina.md`, `architecture.md`, `technology-stack.md`, `intent.md`, and relevant dependency-admission boundaries. The table accounts for every topic in the former Limina record. It records corrections without preserving a second current definition.

| Old knowledge / original problem                                                                                             | Action and correction                                                                                                                                                              | Current owner / source evidence                                            |
| ---------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| The map broadly called persistent context verified                                                                           | **Narrowed**: Each record has its own evidence scope; PCR is not proof of current correctness                                                                                      | README; entry evidence levels                                              |
| Package version `0.2.3`                                                                                                      | **Removed a duplicated value prone to drift**: The current manifest was `0.3.0`; use the manifest over time                                                                        | Entry; `packages/limina/package.json`                                      |
| Public commands, exports, config object/promise/function                                                                     | **Retained and condensed**: Link command and schema owners without maintaining a second inventory of all flags                                                                     | Entry; `index.ts`, `cli/factory.ts`, `config/loader.ts`                    |
| Default pipeline expressed as an ordered list                                                                                | **Corrected**: Default steps are independent, named steps ordered; materialization and generations are prerequisites/boundaries, without implying all phases are sequential        | System model; `pipeline/steps.ts`, `plan.ts`, executor                     |
| Flat checker namespace, named scope, auto discovery                                                                          | **Retained** with selection and closure conditions alongside                                                                                                                       | Entry, system model; checker config/discovery/explicit ownership           |
| Authority freezes before all solution constraints                                                                            | **Narrowed**: Named solution authoritative closure already exists during discovery; promotion/coloring/finalization follow freeze                                                  | Semantics, I02; `checker-ownership-resolution.ts` / discovery              |
| Separation of semanticAuthority and finalOwner                                                                               | **Retained with a causal explanation**: Freeze is a value snapshot plus validation, not an invented deep freeze                                                                    | I02; types / semantic-authority / generated-graph tests                    |
| Locked native provider uses only root-facts and forbids external/lib/reference closures, while later prose says full bounded | **Rewrote the contradiction**: The provider creates a default full bounded Program; importer roots remain constrained; root-facts is a separate mode                               | Semantics, I03; `provider.ts`, `context.ts`, `admission.ts`                |
| Native facts snapshots are boundary-independent                                                                              | **Corrected**: Workspace boundary is part of fact identity and affects external ambient closures; this is not a pure syntax cache                                                  | Lifecycle, I09; `typescript-semantic/identity.ts`, native reproduction     |
| Native raw resolution, ambient evidence, referenceRequirement                                                                | **Retained with separate layers**: Four fields must not collapse; ambient+membership and augmentation have concrete counterexamples                                                | Semantics, I05; `dependency-fact.ts`, native repair tests                  |
| All locked framework authority comes only from generated representations                                                     | **Narrowed**: Framework generated paths coexist with native TS channels; ordinary TS occurrences need not pass through an SFC map                                                  | Semantics; checker-resolution-provider / preparation-source                |
| Oxc globally cannot produce graph/source evidence                                                                            | **Narrowed** to locked project-dependency targets; pending physical candidates and runtime inspection have their own call contracts                                                | Semantics, I04; checker-resolution-provider / direct-dependency-record     |
| PreparedDependencyFact provenance, path/kind, strict mapping                                                                 | **Retained** with resolvedBy, target kind, and TypeEvidence explained separately                                                                                                   | Semantics, I04; dependency-record / framework contracts                    |
| Long framework tuple version lists                                                                                           | **Moved to executable source owner references**: Adapter admission does not mean every version was tested; leaf/toolchain scope remains on the semantics page                      | Semantics; checker compatibility/toolchain files                           |
| Historical Astro sharp/CI license rationale mixed into semantic authority                                                    | **Moved out of semantic definitions**: Link workspace overrides and Dependency Review policy; do not carry forward design motives without new evidence or authorize new exceptions | Workflow; `pnpm-workspace.yaml`, `.github/workflows/dependency-review.yml` |
| Vue/Astro/Svelte hosts, native routes, strict maps, Svelte mode and Windows map paths                                        | **Retained with narrower** toolchain capabilities; explicitly describe Vue's process-wide slot; Svelte does not represent the complete preprocess/config lifecycle                 | Semantics / lifecycle; framework source and tests                          |
| Effective roots, relative types, compiler-requested JSX                                                                      | **Retained** with actual compiler requests explicit; do not infer them from JSX mode alone                                                                                         | Semantics, I03; effective-roots/module-records/compiler-overrides          |
| Generated configs, source solutions/leaves, output projection                                                                | **Retained**: Raw references are semantic inputs, not permission for source leaf references at the governance layer                                                                | System model / semantics; solution-role/generated config readers           |
| Declaration-provider / framework-schedule / dts endpoints                                                                    | **Retained** with raw refs / implicitRefs / solution equality added, removing the possible implication that every edge starts from an import                                       | System model, I06–I07; reference-recording/framework inference/coloring    |
| One canonical path or generation governs every identity                                                                      | **Corrected**: Lexical config paths differ from physical package/mutation identity; analysis and provider generations differ                                                       | System model / lifecycle; utils/path, manager, namespace                   |
| Raw / authority packages, regions, optional names                                                                            | **Retained** with explicit physical alias guard scope                                                                                                                              | System model, I01; workspace validation                                    |
| Validation domains, resource physical/type/package conditions, proof coverage, package output selection                      | **Retained with phase gates explained**: Configured checks do not establish every property across the repository                                                                   | System model, I08; graph/source/proof/package runners                      |
| Graph export view and preflight ownership                                                                                    | **Retained**: A graph document has no task/resource/cache authority; internally owned and borrowed lifetimes differ                                                                | System model / lifecycle; dependency-graph setup/collection                |
| Check snapshot v7 / source v1                                                                                                | **Corrected**: Current check is v8; source and standalone invocation use distinct v1 schemas                                                                                       | Lifecycle, I12; snapshot/types, invocation-snapshot                        |
| Attempt sequence, last-run/completion digest, no fallback to old results                                                     | **Retained**, scoped to published attempts; config/plan failure may precede publication                                                                                            | Lifecycle, I12; attempt IO/query and CLI tests                             |
| Namespace, manifest v5, writer lease, replan, marker/recovery                                                                | **Retained and narrowed** to the corresponding managed materialization; internal unrevisioned plans, raw/export/migration do not inherit these guarantees                          | Lifecycle, I10–I11; namespace/plan/materializer                            |
| Migration JSONC, dirty Git, hardlinks, non-atomic in-place writes, preservation on drift                                     | **Moved** to a complete explanation on the lifecycle page, without merging migration and materializer into one transaction                                                         | Lifecycle; commands/migration/transaction                                  |
| Cross-platform fixtures, CLI process budgets, Corepack cache                                                                 | **Moved** to supporting workflow traps with source/test owners retained; not promoted to core properties                                                                           | Workflow; path helpers, detector environment, CLI/recovery tests           |
| Root architecture and technology records duplicate package-local definitions                                                 | **Retained repository integration**; the default pipeline only links to the system model                                                                                           | Architecture / technology-stack                                            |
| Independent release units in intent                                                                                          | **Retained unchanged**: No conflict was found in manifest/release unit facts; long-term product relationships remain undecided                                                     | Intent; package/release configuration                                      |
| “Human direction” list inferred from Codex memory                                                                            | **Removed its attribution to human direction**: Workflow records the operating mechanism explicitly authorized for this task; long-term compatibility/extension intent stays Open  | Entry; this user request, without inventing an existing vouch              |

## Four rounds of adversarial architecture review

These reviews challenged the completed record set. Static refutation and runtime counterexamples are recorded separately; repeating one test three times is not independent validation.

| Round / attack target     | Actual refutation path                                                                                                                                                                                                                         | Findings and model corrections                                                                                                                                                                                                                                                   |
| ------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1 Authority               | Traced each mutation in `checker-ownership-resolution`; compared named solution discovery with frozen checks; searched for production callers of ArchitectureValidationWorkflow; ran ownership/generated-graph regressions                     | Later final checker selection does not rewrite semantics; solution constraints cannot all be placed after freeze; the typed validation registry is not yet the unified CLI entry point; locked types are not runtime authentication. Phase and authority claims were corrected   |
| 2 Identity / relation     | Compared path helpers with canonical package/namespace identity; traced native fact→recording→edge→color→SCC; challenged with ambient/paths/included/external/augmentation and external symlink closures                                       | Lexical path≠realpath; ambient may carry compiler-membership; resource observation≠TypeEvidence; implicitRefs/solution equality need no import. Removed explanations treating all dependencies as one edge kind                                                                  |
| 3 Generation / lifecycle  | Traced executor command settlement→join→advance, manager replanning, and slot promise identity; inspected Vue shared slots, custom providers, and disposal; ran snapshot/token/symlink reproductions and materialization/preflight regressions | Provider generation may advance independently; Vue slots are shared across managers; custom providers do not automatically become defaults; caches do not universally include content; ensure-after-dispose has no uniform rejection. I09 was downgraded to Partially executable |
| 4 Knowledge / enforcement | Accounted for all old PCR topics with keep/narrow/rewrite/move/remove/open; checked source links, Mermaid ordering, and the evidence matrix; read the architecture-boundaries scanner's matching scope                                         | Did not claim whole-program acyclicity from local AST guards or future intent from passing tests; deduplicated entry/schema/tuple content; actually removed stale v7/root-facts/boundary-independent claims                                                                      |

Affected conclusions were rechecked: I02 against frozen+final owner combinations; I03/I05/I09 through three complete external rounds after correction; I06/I07 against implicit/solution positive paths and declaration negative paths together; I09–I12 against existing lifecycle/mutation/query regressions. Current exceptions were not erased to make the model appear uniform.

## Findings

| ID  | Severity / disposition / category                                              | Evidence, impact, and treatment                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --- | ------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| F01 | **P1 / BLOCKING (old knowledge publication, corrected) / documentation drift** | Direct contradictions around root-facts/full Program and boundary-independent facts could mislead reference repairs and cache design. Production provider/identity code and native reproductions refuted the old prose. Current owners have been rewritten                                                                                                                                                                                                                     |
| F02 | **P2 / NON-BLOCKING / architecture risk + missing enforcement**                | Preflight ensure-after-dispose lacks a uniform runtime gate; external cache reuse lacks universal content versions; internally created pipeline preflight lacks the CLI's disposal in finally. CLI check/standalone and owned graph export release explicitly; misuse after current CLI completion was not established. Before repeated direct internal API calls or daemon support, define ownership/lifecycle contracts, then add entry rejection and mutation-version tests |
| F03 | **P2 / NON-BLOCKING / unresolved integration boundary**                        | The application-level typed ArchitectureValidationWorkflow exists without a current production caller; direct runners coexist with the abstraction. Ideal wiring cannot be recorded as fact. A human must decide whether to unify them and the migration scope; this task neither wires nor deletes it                                                                                                                                                                         |
| F04 | **P3 / NON-BLOCKING / partial architecture guard**                             | architecture-boundaries covers only the relative static runtime imports it parses and selected named calls; aliases/dynamic/namespace forms are not generally traced. No concrete missed cycle causing an actual error was found. Add targeted counterexamples when introducing these syntax boundaries without freezing directory layout                                                                                                                                      |
| F05 | **P3 / NON-BLOCKING (corrected) / documentation drift**                        | Stale manifest version and snapshot v7, the root map's broad verified claim, duplicated tuples, and memory attributed as human intent were replaced with source links, v8/separate schemas, evidence scopes, and Open questions respectively                                                                                                                                                                                                                                   |
| N01 | **NO DEFECT / semantic distinction**                                           | TypeScript semantic authority + vue-tsc final owner, ambient+compiler-membership, concrete output without backward source inference, and pure scheduling SCCs are valid current semantics, not inconsistencies to repair                                                                                                                                                                                                                                                       |
| N02 | **NO DEFECT / validation environment**                                         | Stderr from 8 failed CLI assertions pointed to tsx `listen EPERM`; the unchanged two-file rerun passed 73 tests after lifting the IPC sandbox restriction. This was not an observed Limina freshness regression                                                                                                                                                                                                                                                                |

This evidence established no production P0/P1 defect; that does not mean the entire system is defect-free. F02–F04 are scoped risks/gaps, without unauthorized production repairs. No executable guards were added: existing guards cover core semantics, while remaining gaps involve undecided contracts or would freeze implementation structure. Native repair tests staged by the user before this task cannot be credited as additions from this task.

## Validation

Environment for this task: macOS arm64, Node **v24.21.0**, pnpm **11.9.0**, TypeScript **6.0.3**, Vitest **4.1.7**, tsx **4.22.4**. Nx targets were actually queried with `NX_DAEMON=false pnpm nx show project limina`. All tests used the current working tree; none switched back to HEAD.

### External minimal reproduction

Location: `~/Project/dev-server-repo/repros/limina-architecture-knowledge-20260911/`. `run.mts` directly imports current Limina source and installed TS to construct ESM/NodeNext fixtures; `results.json` retains raw facts/observations. Command, with docs-islands as cwd:

```sh
node --import tsx /Users/chenjiaxiang/Project/dev-server-repo/repros/limina-architecture-knowledge-20260911/run.mts
```

| Experiment         | Intent / independence from other rounds                                                                   | Conditions and results                                                                                                                                                                      |
| ------------------ | --------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Baseline           | Challenge whether resolution can differ from admission                                                    | `main.ts` imports local source outside roots/references; the workspace boundary contains the provider. Resolved checker-source + source-semantic, admission excluded                        |
| Round 1            | Change type provision, resolution, and compiler membership without changing lifecycle                     | Ambient+paths→compiler-membership; explicit included/external controls→null; augmentation→checker-source/source-semantic. Independent TS Program symbol checks agreed                       |
| Round 2            | Change root provenance and occurrence production channels, independent of Round 1's ambient case          | Extends-relative types become importers; react-jsx/react-jsxdev/react/preserve plus jsxImportSource agree with actual TS host synthetic requests, without predicting from JSX mode          |
| Round 3            | Change physical topology, workspace admission boundary, and cache key, independently of symbol/JSX inputs | preserveSymlinks false/true × inner declaration governed false/true: ambient→missing; a shared cache retains two boundary-distinct fact sets; no virtual source edge                        |
| Lifecycle controls | Change source text, object tokens, and symlink mutation paths                                             | Disposed live context rejects methods; historical snapshot retains a.js while a new context gets b.js; same-number namespace rejects cross-token plan; nested symlink authority is rejected |
| CLI control        | Exercise the real binary and persisted query                                                              | After publishing an aborted attempt, JSON query returns exit1/status aborted without returning old inventory                                                                                |

The initial Round 3 incorrectly treated a `resource` observation as TypeEvidence, and its assertion was refuted. Source showed that `virtual:` runtime classification can also produce resource. The proposition was corrected to “admission boundary changes native evidence, the cache isolates it, and no source edge is generated”; the complete baseline + three rounds were then rerun and passed. The first CLI probe also imported the wrong barrel; switching to the production `check-attempt-io` entry succeeded. This was a harness error, not a product defect.

These results establish only the listed native/lifecycle conditions. TS 6.0.3 experiments do not establish equivalence across all supported TS/framework versions. Framework capability conclusions are primarily source-established, supplemented by repository regressions for the currently installed tuples.

### Repository regression commands

```sh
NX_DAEMON=false pnpm nx run limina:test:unit -- src/__tests__/architecture-boundaries.spec.ts src/__tests__/native-reference-repair.spec.ts src/__tests__/project-dependencies.spec.ts src/__tests__/preflight.spec.ts src/__tests__/mutation-boundary.spec.ts src/__tests__/materialization-recovery.spec.ts src/__tests__/check-attempt.spec.ts src/__tests__/invocation-snapshot.spec.ts
NX_DAEMON=false pnpm nx run limina:test:unit -- src/__tests__/generated-graph.spec.ts src/__tests__/typescript-semantic-context.spec.ts src/__tests__/workspace-validation.spec.ts src/__tests__/execution.spec.ts src/__tests__/svelte-semantic.spec.ts src/__tests__/vue-semantic.spec.ts src/__tests__/astro-semantic-resolution.spec.ts
NX_DAEMON=false pnpm nx run limina:test:unit -- src/__tests__/check-attempt.spec.ts src/__tests__/execution.spec.ts
NX_DAEMON=false pnpm nx run limina:test:unit
```

The first two groups returned **66 passed / 7 failed / 1 skipped** and **314 passed / 1 failed** respectively. Rerunning the two files unchanged inside the sandbox still failed. A temporary child_process diagnostic preload captured `tsx listen EPERM` from 7 check-attempt child processes. After lifting that IPC restriction, the unchanged two-file rerun returned **73 passed**. The diagnostic preload lived in an external temporary directory and did not modify tests; the successful formal rerun used no preload. `node packages/limina/bin/limina.js --help` succeeded; the external fixture's `check --issues --format json` returned the expected exit1/aborted.

The complete unit suite finished on the same working tree with the IPC restriction lifted: **92 files passed, 1873 tests passed, 2 skipped**. The skipped cases were Windows-only mutation-boundary and standalone-invocation cases; those branches were not covered on this machine. Documentation checks: Prettier check passed for 11 changed files; a Markdown parser checked 217 relative links and heading anchors with no errors. The nodes/edges/ordering of 4 Mermaid diagrams were manually checked against source; no Mermaid renderer was run. Non-fixing ESLint actually returned 11 ignored warnings: current configuration does not check these Markdown files, so this is not lint coverage. The first Prettier check found 8 formatting issues; only external drafts were formatted before the check passed. Integration/typecheck/build/smoke/package/release/Limina check were not run: this task changed only PCR and maintenance guidance, not governed source/config/tests, and did not expand its implementation scope to the user's existing staged changes. The complete unit suite challenged the cross-domain model; it does not replace the checks that were not run.

## Changes and ownership

`limina.md` was rewritten as an entry point, with six new PCR topics having distinct responsibilities. The README map and root architecture/technology routes were updated and deduplicated; package AGENTS gained an invariant impact route for future changes. `intent.md` and dependency-admission retained their original text.

Final content/index comparison passed: of 3116 baseline files, excluding the 5 existing paths modified in this task left 3111 unchanged content fingerprints. `git ls-files --stage` matched the baseline exactly. Only the expected 6 new PCR files existed, with no unexpected untracked files. The user's EN/ZH articles, all production/tests, and a.ts/b.ts were preserved unchanged. New documentation from this task was not staged. `git diff --check` and `git diff --cached --check` both passed, and final `git status --short` was inspected. Temporary evidence logs do not form new repository authority; reproduction paths enable reruns rather than replacing source/test anchors.

## Documentation and boundary check commands

The following `FILES` lists the 11 files actually checked in this task and can be reused for reruns. No broad formatting or fixing lint was run.

```sh
FILES=(.agents/docs/README.md .agents/docs/architecture.md .agents/docs/technology-stack.md .agents/docs/limina.md .agents/docs/limina-system-model.md .agents/docs/limina-semantics.md .agents/docs/limina-lifecycle.md .agents/docs/limina-invariants.md .agents/docs/limina-architecture-workflow.md .agents/docs/limina-architecture-audit.md packages/limina/AGENTS.md)
pnpm exec prettier --check "${FILES[@]}"
pnpm exec eslint "${FILES[@]}"
node /tmp/limina-architecture-20260911/validate-docs.mjs --live
python3 /tmp/limina-architecture-20260911/verify-preservation.py
git diff --check
git diff --cached --check
git status --short
```

Diagnostic reruns additionally used `NODE_OPTIONS=--require=/tmp/limina-architecture-20260911/capture-child.cjs NX_DAEMON=false pnpm nx run limina:test:unit -- src/__tests__/check-attempt.spec.ts` solely to capture failed child-process stderr. External draft formatting used `pnpm exec prettier --write --parser markdown --config /Users/chenjiaxiang/Project/docs-islands/.prettierrc.json`, with arguments limited to this task's temporary drafts. Final source claims were checked by tracing callers/fields with `rg` and reading production implementation; these static structural results are separately labeled Source-established.
