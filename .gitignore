.DS_Store
dist
coverage
.pnpm-store
.nx/
.smoke
.limina/
.vercel/
node_modules
.eslintcache
cache

# ignore the temporary config.ts build output, 
# when vitepress fails to build config.ts, 
# it will remain in the workspace.
**/docs/.vitepress/config.ts.timestamp-*.mjs

# The skills for AI tools are provided by the @docs-islands/agents subpackage 
# and are injected during the post-hook stage of pnpm install.
.claude/
.cursor/
.agent/

# Local environment overrides
*.local
