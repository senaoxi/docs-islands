{
  "$schema": "https://json.schemastore.org/tsconfig",
  // Shared VitePress local defaults. Package tsconfig.json is an IDE solution.
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "isolatedDeclarations": false,
    "declaration": false,
    "lib": ["ESNext", "DOM"],
    "types": ["node"]
  }
}
