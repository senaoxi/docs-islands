{
  // Smoke-local source/editor check. Dist consumer pipeline owns this config.
  "extends": "../../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ESNext", "DOM", "DOM.Iterable"],
    "types": ["node"],
    "allowJs": true,
    "isolatedDeclarations": false,
    "declaration": false
  },
  "include": ["eslint.config.mjs", "playwright.config.ts"],
  "exclude": ["node_modules", "coverage"]
}
