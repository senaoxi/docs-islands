name: "Build and Quality Check"
description: "Build packages and run quality checks (format, lint, check, publint)"

inputs:
  skip-build:
    description: "Skip build step (e.g., when using pre-built artifacts)"
    required: false
    default: "false"
  run-format:
    description: "Run format check"
    required: false
    default: "true"
  run-lint:
    description: "Run lint check"
    required: false
    default: "true"
  run-check:
    description: "Run type check"
    required: false
    default: "true"
  run-lint-packages:
    description: "Run package lint check"
    required: false
    default: "true"

runs:
  using: "composite"
  steps:
    - name: Build Packages
      if: inputs.skip-build != 'true'
      shell: bash
      run: pnpm build
      env:
        DOCS_ISLANDS_MODE: production

    - name: Format Check
      if: inputs.run-format == 'true'
      shell: bash
      run: pnpm format

    - name: Lint
      if: inputs.run-lint == 'true'
      shell: bash
      run: pnpm lint

    - name: Type Check
      if: inputs.run-check == 'true'
      shell: bash
      run: pnpm check

    - name: Lint Packages
      if: inputs.run-lint-packages == 'true'
      shell: bash
      run: pnpm lint:packages
